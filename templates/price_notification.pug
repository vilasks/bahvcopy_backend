html 
    body 
        table 
            tbody 
                tr 
                    td(style={'text-align':'center'})
                        h2 #{stock.SYMBOL} LTP is #{stock.CLOSE}
                        sub as on #{new Date().toDateString()}
                tr 
                    td(style={})
                        img(src=`${stock.img}` style={'max-width':'100%','max-height':'100%'})
